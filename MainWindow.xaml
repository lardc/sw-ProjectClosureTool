<Window x:Class="TestMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestMVVM"
        xmlns:col="clr-namespace:System.Collections;assembly=mscorlib"
        mc:Ignorable="d"
        
        Title="MainWindow" Width="436">
    <Window.DataContext>
        <local:WindowBind/>
    </Window.DataContext>
    <Grid Margin="10,10,10,10">
        <Grid HorizontalAlignment="Center" Margin="0,17,0,0" VerticalAlignment="Top" Width="243">
            <Button Content="Button" VerticalAlignment="Top" Click="Button_Click" Height="23" HorizontalAlignment="Right" Width="54"/>
            <TextBox
                x:Name="textBox"
                Height="21" Margin="-5,0,59,0"
                TextWrapping="Wrap"
                Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                VerticalAlignment="Center"/>
        </Grid>
        <DataGrid
            ItemsSource="{Binding Models}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" SelectionChanged="DataGrid_SelectionChanged" Margin="0,60,0,5" HorizontalAlignment="Center"/>
    </Grid>
</Window>